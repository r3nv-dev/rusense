{
  "$schema": "gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Janela principal: API core do Tauri (invoke/eventos).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
